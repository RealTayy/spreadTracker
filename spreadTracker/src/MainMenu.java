import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;

public class MainMenu {
    String projectLocation = System.getProperty("user.dir");

    public static void main(String[] args) throws Exception {
        SpreadResults spreadResults = new SpreadResults();
        System.out.println("FINISH!");
    }
}

// Need to read teams by City Abb instead... cause NYJ and shit. Maybe make a team ID instead?
// Post Season 2017 has no games need to make sure it comes back as incomplete when pinged for completion

